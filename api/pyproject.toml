[project]
name = "open-deepwiki-api"
version = "1.0.0"
description = "Backend API for DeepWiki."
license = {text = "MIT License"}
requires-python = ">=3.11"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
